[project]
name = "pydns"
version = "0.1.0"
description = "DNS ~ Direct Numerical Simulation"
requires-python = ">=3.10"

dependencies = [
    "matplotlib",
    "numpy",
    "pandas",
    "pillow",
    "polars",
    "pyarrow",
    "pyqt6",
    "meson",
    "ninja",
    "pyinstaller",
]